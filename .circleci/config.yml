version: 2.1
jobs:
  test:
    executor:
      name: node/default
      tag: '13'
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: yarn jest
          name: Run YARN tests
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - test
